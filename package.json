{
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build",
    "docs:preview": "vitepress preview",
    "postinstall": "chmod +x node_modules/.bin/vitepress && chmod +x node_modules/vite/bin/vite.js",
    "build": "vitepress build --outDir dist"
  },
 "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  },
  "dependencies": {
    "@vercel/speed-insights": "^1.2.0"
  },
  "engines": {
    "node": "22.X" 
  }
}
